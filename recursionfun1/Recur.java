import static java.lang.System.out;

public class Recur {
    /**
     * # 1
     * This method will calculate the number of ears on
     * `bunnies` number of bunnies (without loops or multiplication).
     * What is the base case - the smallest possible solution you can solve now?
     */
    public static int numEars(int bunnies) {

        return 0;
    }

    /**
     * # 2
     * This method will print a countdown as shown below
     * (without using a loop):
     * countdown(5) should print: 5, 4, 3, 2, 1, blastoff!
     */
    public static void countdown(int num) {

    }

    /**
     * # 3
     * This method will recursively calculate the factorial
     * of `num`.
     */
    public static int factorial(int num) {

        return 0;
    }

    /**
     * # 4
     * Use recursion to complete this method!
     * This method will return a String that is
     * `str` repeated `i` number of times.
     */
    public static String cheerlead(String str, int i) {

        return "";
    }

    /**
     * # 5
     * This method recursively computes the value of one
     * number to the power of another number.
     * Hint: if exponent is 0, return 1; otherwise, break
     * up the problem into smaller pieces.
     */
    public static int pow(int base, int exp) {

        return 0;

    }

    /**
     * # 6
     * This method recursively computes the nth number in
     * the Fibonacci sequence.
     * The first two numbers in the sequence are 0 and 1.
     * Then, each subsequent number is the sum of the previous two.
     * Ex: 0, 1, 1, 2, 3, 5, 8, 13, 21,...
     */
    public static int fibo(int n) {

        return 0;
    }

    /**
     * # 7
     * This method will recursively print the following pattern:
     * pattern(16) should print: 16, 11, 6, 1, -4
     * pattern(10) should print: 10, 5, 0, -5
     */
    public static void pattern(int num) {

    }

    /**
     * # 8
     * This method recursively computes the number of 'a' characters
     * in `str`.
     * Remember - No Loops!
     */
    public static int countNumA(String str) {

        return 0;
    }

    /**
     * # 9
     * This method will recursively print all the numbers from `a` to
     * `b` in increments of `c` (separated by a space).
     * You may assume that this is always possible (i.e., the numbers
     * supplied by the user will always conform to expectations).
     */
    public static void printAtoBbyC(int a, int b, int c) {

    }

    /**
     * # 10
     * This method recursively counts how many digits of `num` are odd.
     */
    public static int countOdds(int num) {

        return 0;
    }

    /**
     * # 11
     * This method recursively computes the sum of all digits in `num`.
     * Assume `num` will always be a non-negative integer.
     */
    public static int sumDigits(int num) {
        
        return 0;
    }
}